# ============================================
# Corporate Living App - Environment Variables
# ============================================
#
# This file contains example environment variables needed to run the app.
# 
# SETUP INSTRUCTIONS:
# 1. Copy this file: In your terminal, run:
#    - On Mac/Linux: cp .env.example .env.local
#    - On Windows (Command Prompt): copy .env.example .env.local
#    - On Windows (PowerShell): Copy-Item .env.example .env.local
#
# 2. Open .env.local in a text editor (VS Code, Notepad, etc.)
#
# 3. Replace the placeholder values (the parts that say "your-xxx") 
#    with your actual credentials from Supabase
#
# 4. Save the file
#
# 5. Restart your development server (npm run dev)
#
# IMPORTANT: Never commit .env.local to git! It contains secret keys.
# The .gitignore file is already set up to ignore it.
#
# ============================================

# --------------------------------------------
# SUPABASE CONFIGURATION (Required)
# --------------------------------------------
# These credentials connect your app to your Supabase database.
# Get these from: https://supabase.com → Your Project → Settings → API
#
# Example of what they look like:
# NEXT_PUBLIC_SUPABASE_URL=https://abcdefghijklmnop.supabase.co
# NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9.eyJpc3MiOiJzdXBhYmFzZSIsInJlZiI6ImFiY2RlZmdoaWprbG1ub3AiLCJyb2xlIjoiYW5vbiIsImlhdCI6MTYwMDAwMDAwMCwiZXhwIjoxOTAwMDAwMDAwfQ.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Your Supabase Project URL
# Find this in: Supabase Dashboard → Settings → API → Project URL
NEXT_PUBLIC_SUPABASE_URL=your-project-url.supabase.co

# Your Supabase Anonymous (Public) Key
# Find this in: Supabase Dashboard → Settings → API → Project API keys → anon public
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key-here

# Your Supabase Service Role Key (KEEP THIS SECRET!)
# Find this in: Supabase Dashboard → Settings → API → Project API keys → service_role
# WARNING: This key has admin access - never share it or commit it to git!
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here

# --------------------------------------------
# EMAIL SERVICE (Optional - for notifications)
# --------------------------------------------
# Choose ONE of the email providers below.
# Uncomment the lines for your chosen provider and add your credentials.

# OPTION 1: Resend (Recommended - easiest setup)
# Sign up at: https://resend.com
# Free tier: 3,000 emails per month
# Get your API key from: Resend Dashboard → API Keys
#
# RESEND_API_KEY=re_your-api-key-here
# EMAIL_FROM=noreply@yourdomain.com

# OPTION 2: SendGrid
# Sign up at: https://sendgrid.com
# Free tier: 100 emails per day
# Get your API key from: SendGrid → Settings → API Keys
#
# SENDGRID_API_KEY=SG.your-api-key-here
# EMAIL_FROM=noreply@yourdomain.com

# OPTION 3: AWS SES
# Set up at: https://aws.amazon.com/ses/
# Pricing: $0.10 per 1,000 emails
#
# AWS_SES_REGION=us-east-1
# AWS_ACCESS_KEY_ID=your-access-key-here
# AWS_SECRET_ACCESS_KEY=your-secret-key-here
# EMAIL_FROM=noreply@yourdomain.com

# --------------------------------------------
# APPLICATION CONFIGURATION (Optional)
# --------------------------------------------

# The URL where your app is running
# Development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# --------------------------------------------
# TROUBLESHOOTING
# --------------------------------------------
# If you see "Supabase is not configured" errors:
# 1. Make sure you created .env.local (not .env.example)
# 2. Make sure you replaced all "your-xxx" placeholders with real values
# 3. Make sure there are no extra spaces around the = signs
# 4. Restart your dev server: Stop it (Ctrl+C) and run 'npm run dev' again
#
# Still having issues? See ENV_SETUP.md for detailed help.
